{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-center">
        <button type="submit" class="mt-6 relative inline-flex items-center justify-center px-4 py-2 text-white bg-blue-700 rounded-lg shadow-md hover:shadow-lg transition duration-300 ease-in-out transform hover:scale-105 active:scale-95 focus:outline-none focus:ring-4 focus:ring-blue-500 bg-blur">
            <span class="mr-2">{{ button_label|default('label.save'|trans) }}</span>
            <div class="arrow-container">
                <div class="arrow"></div>
            </div>
        </button>
    </div>
{{ form_end(form) }}
