{% extends 'base.html.twig' %}

{% block title %}Championship{% endblock %}

{% block body %}
    <h1>Championship</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ championship.id }}</td>
            </tr>
            <tr>
                <th>BlueGoal</th>
                <td>{{ championship.blueGoal }}</td>
            </tr>
            <tr>
                <th>GreenGoal</th>
                <td>{{ championship.greenGoal }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ championship.state }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_championship_index') }}">back to list</a>

    <a href="{{ path('app_championship_edit', {'id': championship.id}) }}">edit</a>

    {{ include('championship/_delete_form.html.twig') }}
{% endblock %}
