<!DOCTYPE html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        {% block title %}{{ 'base.welcome'|trans }} !{% endblock %}
    </title>
    <link rel="icon" href="{{ asset('Images/logo.png') }}">
    {% block stylesheets %}
        <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    {% endblock %}
</head>
<body class="bg-gray-100">
    {% for message in app.flashes('notice') %}
        <div class="flash-notice">
            {{ message }}
        </div>
    {% endfor %}

    {% set route = app.request.attributes.get('_route') %}
    {% set route_params = app.request.attributes.get('_route_params') %}
    {% set params = route_params|merge(app.request.query.all) %}

    <div class="min-h-full">
        <nav class="bg-gradient-to-r from-blue-500 via-teal-400 to-indigo-600 flex items-center justify-between px-6 py-4 shadow-lg rounded-b-xl">
            <!-- Logo -->
            <div class="flex items-center space-x-3">
                <img class="w-12 h-12 rounded-full shadow-lg" src="{{ asset('Images/logo.png') }}" alt="Your Company">
                <p class="text-4xl font-bold text-white">RobotLeague</p>
            </div>

            <!-- Menu Burger (affiché uniquement sur les écrans moyens) -->
            <div class="sm:block md:hidden lg:hidden">
                <label for="menu-toggle" class="cursor-pointer">
                    <svg class="w-6 h-6 text-white transition-all hover:text-blue-200" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <svg class="w-6 h-6 text-white transition-all hover:text-blue-200" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                    </svg>
                </label>
                <input type="checkbox" id="menu-toggle" class="hidden" />
            </div>

            <!-- Menu Desktop (affiché uniquement sur les grands écrans) -->
            <div class="hidden md:flex lg:flex space-x-6">
                <a href="{{ path('app_home') }}" class="text-lg font-semibold text-white hover:bg-white hover:text-blue-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Menu</a>

                <!-- Menu Équipes -->
                <div class="relative">
                    <button class="text-lg font-semibold text-white hover:bg-white hover:text-blue-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Équipes</button>
                    <div class="dropdown-content absolute hidden bg-white text-black shadow-md rounded-md mt-2 w-48">
                        {% if app.user %}
                            {% if is_granted('ROLE_USER') %}
                                {% if app.user.myTeam == null %}
                                    <a href="{{ path('app_team_new') }}" class="block px-4 py-2 hover:bg-blue-100 rounded-md transition-all">Créer une équipe</a>
                                {% else %}
                                    <a href="{{ path('app_team_member', {'id': app.user.myTeam.id}) }}" class="block px-4 py-2 hover:bg-blue-100 rounded-md transition-all">Mon équipe</a>
                                {% endif %}
                            {% endif %}
                        {% endif %}
                        <a href="{{ path('app_team_show') }}" class="block px-4 py-2 hover:bg-blue-100 rounded-md transition-all">Voir les équipes</a>
                    </div>
                </div>

                <!-- Menu Admin (affiché uniquement pour les organisateurs) -->
                {% if is_granted('ROLE_ORGANISATEUR') %}
                    <div class="relative">
                        <button class="text-lg font-semibold text-white hover:bg-white hover:text-blue-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Admin</button>
                        <div class="dropdown-content absolute hidden bg-white text-black shadow-md rounded-md mt-2 w-48">
                            <a href="{{ path('app_user_index') }}" class="block px-4 py-2 hover:bg-blue-100 rounded-md transition-all">Utilisateurs</a>
                            <a href="{{ path('app_championship_index') }}" class="block px-4 py-2 hover:bg-blue-100 rounded-md transition-all">Championnat</a>
                        </div>
                    </div>
                {% endif %}

                {% if app.user %}
                    <a href="{{ path('app_logout') }}" class="text-lg font-semibold text-white hover:bg-white hover:text-red-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="text-lg font-semibold text-white hover:bg-white hover:text-blue-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Connexion</a>
                    <a href="{{ path('app_register') }}" class="text-lg font-semibold text-white hover:bg-white hover:text-blue-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Inscription</a>
                {% endif %}
            </div>
        </nav>

        {% block body %}
        {% endblock %}
    </div>

    <!-- Menu mobile -->
    <div class="sm:hidden fixed inset-0 bg-black bg-opacity-50 backdrop-blur z-50 hidden" id="menu-mobile">
        <div class="flex flex-col items-center justify-center h-full space-y-6">
            <a href="{{ path('app_home') }}" class="text-white text-lg font-semibold hover:bg-teal-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Menu</a>
            <a href="{{ path('app_team_show') }}" class="text-white text-lg font-semibold hover:bg-teal-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Equipes</a>
            {% if app.user %}
                {% if is_granted('ROLE_USER') %}
                    {% if app.user.myTeam == null %}
                        <a href="{{ path('app_team_new') }}" class="text-white text-lg font-semibold hover:bg-teal-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Créer une équipe</a>
                    {% else %}
                        <a href="{{ path('app_team_member', {'id': app.user.myTeam.id }) }}" class="text-white text-lg font-semibold hover:bg-teal-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Mon équipe</a>
                    {% endif %}
                {% endif %}
                {% if is_granted('ROLE_ORGANISATEUR') %}
                    <a href="{{ path('app_user_index') }}" class="text-white text-lg font-semibold hover:bg-teal-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Utilisateurs</a>
                    <a href="{{ path('app_championship_index') }}" class="text-white text-lg font-semibold hover:bg-teal-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Championnat</a>
                {% endif %}
                <a href="{{ path('app_logout') }}" class="text-white text-lg font-semibold hover:bg-red-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Déconnexion</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="text-white text-lg font-semibold hover:bg-teal-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Connexion</a>
                <a href="{{ path('app_register') }}" class="text-white text-lg font-semibold hover:bg-teal-600 px-4 py-2 rounded-md transition duration-300 ease-in-out transform hover:scale-105">Inscription</a>
            {% endif %}
        </div>
    </div>





<div class="relative">
    <div class="fixed top-0 left-0 z-50">
        <a class="text-blue-100" href="{{ path(route, params|merge({ _locale: 'en' })) }}">English</a>
        <a href="{{ path(route, params|merge({ _locale: 'fr' })) }}">Français</a>
    </div>
</div>

<div>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const menuToggle = document.getElementById('menu-toggle');
            if (menuToggle) {
                menuToggle.addEventListener('change', function () {
                    const menu = document.getElementById('menu-mobile');
                    if (menu) {
                        if (this.checked) {
                            menu.classList.remove('hidden');
                        } else {
                            menu.classList.add('hidden');
                        }
                    }
                });
            }

            // Gestion des menus déroulants
            const menuButtons = document.querySelectorAll('.relative button');
            menuButtons.forEach(button => {
                button.addEventListener('click', function () {
                    // Fermer tous les autres menus
                    const allDropdowns = document.querySelectorAll('.dropdown-content');
                    allDropdowns.forEach(dropdown => {
                        if (dropdown !== this.nextElementSibling) {
                            dropdown.classList.add('hidden');
                        }
                    });

                    // Afficher/masquer le menu actuel
                    const dropdown = this.nextElementSibling;
                    dropdown.classList.toggle('hidden');
                });
            });
        });
    </script>

	

</body>
</html>
